/***
-----------------
"Addition" monoid
-----------------

A "add" monoid ''M'' is an abelian monoid with the syntaxic
shortcuts of an "add" magma with the following additional
shorcuts.

    1) The neutral element is "zero" : you can use ''M.zero''
    instead of ''M.id''.

    1) ''M.ntimes'' instead of ''M.op_ntimes'' to apply 'n'
    times ''M.op''.
***/

type MonoidAdd : Monoid + MagmaAdd {
  alias zero   := id ,
        ntimes := op_ntimes
}
