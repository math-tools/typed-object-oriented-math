/***
----------------
"Addition" group
----------------

A "add" group is an abelian group with the syntaxic shortcuts of
an "add" monoid.
***/

type GroupAdd : Group + MonoidAdd {
  alias oppo := inv
}
