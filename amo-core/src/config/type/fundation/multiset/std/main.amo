/***
Set

A multiset ''MS'' is a specific set which can have "repeated" occurence
of the same objects.
***/

type MultiSet : Set as MS {
  func occu Unspecified -> \N

  fix func in (elt : Unspecified) -> \B {
    return (occu elt) != zero
  }
}
