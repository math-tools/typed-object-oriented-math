/***
???
***/

type OrderedSet : Set as OS {
  func lt (elt_1 : InSet) -> (elt_2 : InSet) -> \B

  axiom {
    anti_symmetry :=
      forall x in OS ,
      not (lt x x)

    transitivity :=
      forall x y z in OS ,
      (lt x y) and (lt y z) ==> (lt x z)
  }
}
