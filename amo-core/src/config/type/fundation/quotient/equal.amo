/***
???

the use of ''cano_surj'' is to make things computabke more easily
***/

with (Qof : QuotientSetOf (PreQT : Set)) in
fix func equal (x : QF.InSet) -> (y : QF.InSet) -> \B {
  return (
    Qof.repr x = Qof.repr y
  )
}
