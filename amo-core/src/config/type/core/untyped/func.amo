/***
A versatile common equality.
***/

fix func equal (obj_1 : Unspecified) -> (obj_2 : Unspecified) -> \B {
  return (same_tokens obj_1 obj_2)

  axiom {
    reflexitivity :=
      forall obj : Unspecified ,
      equal obj obj = true
  }
}

parse "{x} = {y}" as (equal x y)
