type MonoidAdd : Monoid + MagmaAdd

    + alias zero : id

    + desc : "Addition" monoid
        A "add" monoid ''M'' is an abelian monoid with the syntaxic
        shortcuts of a "add" magma. The neutral element is "zero" :
        you can use ''M.zero'' instead of ''M.id''.
