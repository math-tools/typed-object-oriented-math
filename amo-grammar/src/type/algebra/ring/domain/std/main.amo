/***
Domain

A domain ''D'' is a ring which has no zero divisor.

note::
    We can prove that ''x != id_plus'' and ''prod x y = prod x z''
    imply ''y = z'' (this implication is essential to build the quotient
    field of an integral domain).

    verbatim::
        prod x y = prod x z

        ==>  { Let ''z_op'' be the ''add'' inverse of z }

        add (prod x y) (prod x z_op)
        =
        add (prod x z) (prod x z_op)

        ==>  { Use of left distribution }

        prod x (add y z_op) = prod x (add z z_op)

        ==> { ''add z z_op = id_plus'' }
            { ''id_plus'' is ''prod'' absorbent }

        prod x (add y z_op) = id_plus

        ==> { Use of the ''no_zero_div'' axiom }

        x = id_plus or (add y z_op) = id_plus

        ==> { ''x != id_plus'' }

        add y z_op = id_plus

        ==> { Unicity of the ''add'' inverse }

        y = z
***/

type Domain : Ring as D {
  axiom {
    no_zero_div :=
      forall x y in D ,
      prod x y = id_plus ==> (x = id_plus or y = id_plus)
  }
}
