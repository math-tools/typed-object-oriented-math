type MagmaProd : MagmaAbelian as M

    + parse "{x} * {y}"
            as
            (op x y)

    + desc
        A "prod" magma amo::''M'' is an abelian magma for which
        amo::''x * y'' means amo::''M.op x y''.
