// TODO!

Record quot_class_of (T Q : Type) := QuotClass { repr : Q -> T;
pi : T -> Q;
reprK : forall x : Q, pi (repr x) = x
}.

The function pi is called the canonical surjection and we call the function
repr the representative function.


 Notation "x = y %[mod qT]" := (pi qT x = pi qT y).





    + ref
        "Pragmatic Quotient Types in Coq [Draft]"
        Cyril Cohen
