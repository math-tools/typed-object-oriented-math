type MagmaPlus : MagmaAbelian as M

    + parse "{x} + {y}" as op x y

    + alias zero : id

    + desc
        A "plus"" magma amo::''M'' is an abelian magma for which
        amo::''x + y'' means amo::''M.op x y''.
        The neutral element is "zero" : you can use amo::''M.zero''
        instead of amo::''M.id''.
