type NatSet : Monoid as N
    + instance : \N
    + alias op_add : N.op_add

    + fubc succ : N -> N

    + axiom
        + XXX : forall x : N    , zero_N != succ x
        + XXX : forall x, y : N , succ x = succ y ==> x = y
        + XXX : forall x, y : N , op_add x (succ y) = succ(op_add x y)
        + XXX : forall x, y : N , op_prod x (succ y) = op_add x (op_prod x y)

    + desc
        ???
        The set of natural numbers is a monoid respecting the Peano axioms.
