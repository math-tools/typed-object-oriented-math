subtype monoid_commutative : monoid as M
    + alias op : M.op
    + axiom
        + commutativity : forall x y : M ,
                          op x y = op y x
    + desc
        A commutative monoid is a monoid respecting the additional property
        given below.
