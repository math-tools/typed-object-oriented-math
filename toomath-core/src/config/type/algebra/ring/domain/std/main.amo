/***
======
Domain
======

-------------
\Gene \struct
-------------

A domain ''D'' is a ring which has no zero divisor.
***/

type Domain : Ring as D {
  axiom {
    no_zero_div :=
      forall x y in D ,
      op_prod x y = id_add ==> (x = id_add or y = id_add)
  }
}
